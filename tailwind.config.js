module.exports = {
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {}
    },
    variants: {
        extend: {}
    },
    plugins: [],
    purge: {
        // mode: 'all',
        // content: ["./src/**/*.svelte"]
    }
}
